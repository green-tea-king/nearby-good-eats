# 在地美食榜固定產品規則

本檔是給未來 Codex / AI agent / 開發者開工前快速對齊用。完整設計細節仍以 `design.md` 為準；若本檔與 `design.md` 衝突，先停止相關修改並回報衝突。

## 產品定位

1. 這是手機直式使用的美食排行榜 Web App。
2. 首頁就是「在地美食榜」，不做額外 landing page。
3. 只維護一種主要手機版型，優先支援 360px 到 480px 寬度。
4. 使用者登入 Google 後才能使用查詢功能。
5. 不使用假餐廳、假評分、死資料或 demo fallback 假裝真實結果。

## 搜尋與 API 原則

1. 首次打開網頁不要先給結果，也不要自動呼叫 Google API；必須等使用者按「套用」後才搜尋。
2. 所有濾網變更只更新畫面狀態，不可每點一個濾網就搜尋。
3. 每次搜尋先顯示 3 張餐廳卡片。
4. 若不足 3 張，走路 / 開車模式要用同心圓逐步放寬距離補滿；仍不足時才顯示最接近條件的候選，不可亂推薦不相干店家。
5. 搜尋結果先只抓前 3 到 10 家必要資料；詳細資料、照片、摘要、AI 判讀只對前幾名或使用者展開詳情時處理。
6. 同一個 `place_id` 必須快取，避免重複查 Place Details。
7. 照片 lazy load，且只放詳情內；卡片主列表不預載照片。
8. Google 摘要、Atmosphere、評論摘要與 AI 判讀都不得對每家無限制抓取。
9. 後續所有修改都要以節省 API 調用與成本為核心考量。

## 濾網規則

1. 濾網永遠位於頁面上方內容區，不使用漢堡選單，不會因外圍點擊、滾動、縮放或拖曳而消失。
2. 濾網可以自然被頁面捲上去，但不應被程式自動隱藏。
3. 地區濾網為縣市 / 區域 / 里。
4. 地區與交通互斥：選地區清掉交通與定位；選交通或定位清掉地區。
5. 交通支援走路 / 開車，定位按鈕放在交通列並顯示定位地點。
6. 預設使用定位點，並以走路可到為主要情境。
7. 走路門檻為 15 分鐘內，開車門檻為 30 分鐘內。
8. 營業預設為營業中，並提供不限 / 營業中。
9. 關鍵字是強條件，例如「麵線」必須真正在店名、類型、Google 摘要或評論摘要命中；不符合時不可亂給建議。
10. 關鍵字支援多詞，例如「滷肉飯 排骨湯」，採 AND 條件。
11. 無結果時提供放寬關鍵字、取消條件或放寬距離，不改塞不相關結果。
12. 評鑑濾網可多選，採 OR 條件，資料來源只讀本地批次檔，不增加即時 Google API。
13. 菜系可作為濾網，但要明確標示判斷來源；沒有可靠來源時不可偽裝成精準硬濾網。

## 排序與評分

1. Google 評分與評論數是排行主體。
2. 評論量權重要夠重；同樣 4 顆星時，評論數多者應明顯更前。
3. 使用貝氏分數、評論量加分、評鑑加分、社群熱度小加分與評論可信度保守扣分。
4. 外部評鑑只做加分，不取代 Google 評分與評論數。
5. 米其林星級高權重，必比登與 500 系列中高權重，50 Best 高權重但只影響少數店。
6. 評鑑、社群聲量、平台口碑都必須有可追溯來源，不可憑印象補資料。

## 卡片與詳情

1. 餐廳卡片第一眼重點：排行、店名、Google 評分、評論數、營業、地區、交通時間、綜合分數、強徽章。
2. 所有頁面的餐廳卡片都必須顯示評分，評分是核心資訊。
3. 主卡最多顯示 4 個強徽章；外部評鑑優先。同一來源有多年份時，主卡只顯示最新 / 最高等級徽章，完整年份來源放詳情。
4. 照片、完整服務標籤、Google 摘要、評論摘要、AI 判讀與完整認證放詳情內。
5. 照片缺漏時不顯示假 placeholder；展開詳情後可補查照片，仍沒有就不顯示。
6. 點詳情照片小圖可以放大。
7. 分享卡片必須有獨立連結與 `?place=<GooglePlaceId>` 路由。
8. 分享失敗時直接 copy link fallback，不跳系統錯誤。

## AI 與外部資料

1. AI 判讀不可在前端假裝；只有後端 proxy 真正回傳時才顯示 AI 判讀。
2. AI 回傳必須包含信心分數、推薦理由與來源欄位。
3. AI API key 不可放前端。
4. 外部資料不做使用者搜尋時即時查詢，採批次更新靜態檔。
5. `assets/external-signals.json` 是外部訊號入口。
6. 評鑑資料只保留 `Michelin`、`Bib`、`Michelin Selected`、`500盤`、`500碗`、`500甜` 六類，放在 `assets/awards-taiwan.json`。
7. 每筆評鑑都必須有年份；無法確認年份時只能標記 `年份待確認`，不可猜測。
8. 愛食記、OpenRice、Tripadvisor 等平台口碑只做手動 / AI 整理後的批次輔助訊號，不自動即時爬取。

## 驗收清單

每次修改完成至少檢查：

1. `node scripts/scan-ui-text.js` 沒有可見亂碼 findings。
2. JavaScript 語法能通過檢查。
3. 手機首屏沒有明顯擠壓、重疊或文字溢出。
4. 初次進站不自動搜尋。
5. 濾網變更不立即搜尋，只有按「套用」才搜尋。
6. 搜尋結果目標為 3 張卡片。
7. 沒有新增假資料或未追溯來源的外部資料。
8. 若部署，必須驗證正式站版本與基本 smoke 結果。
